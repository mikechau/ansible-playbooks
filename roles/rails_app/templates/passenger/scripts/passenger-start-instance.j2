#!/usr/bin/env bash

# AUTOGENERATED BY ANSIBLE
# NOT CHECKED INTO SOURCE CONTROL

set -x

{{ rails_app_current_release_path }}/bin/passenger start --instance-registry-dir {{ item.invocation.module_complex_args.path }} --socket {{ rails_app_shared_run_path }}/{{ rails_app_name }}.{{ item.item }}.r{{ rails_app_current_release_id }}.socket --pid-file {{ rails_app_shared_run_path }}/{{ rails_app_name }}.{{ item.item }}.r{{ rails_app_current_release_id }}.pid
