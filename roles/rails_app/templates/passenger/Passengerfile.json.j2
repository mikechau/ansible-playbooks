#jinja2: lstrip_blocks: True

// AUTOGENERATED BY ANSIBLE
// NOT CHECKED INTO SOURCE CONTROL

{
  {% for (key, value) in rails_app_passengerfile_config.iteritems() %}
  "{{ key }}": {{ value }}{% if not loop.last %},{% endif %}

  {% endfor %}
}
